import CategoryPage from './CategoryPage'

function FeaturesPage() {
  return (
    <CategoryPage
      title="Features"
      description="This section will populate automatically when features components are registered."
      category="Features"
    />
  )
}

export default FeaturesPage
