import PagePlaceholder from './PagePlaceholder'

function HomePage() {
  return (
    <PagePlaceholder
      title="Welcome to ALai"
      description="Browse the component library, start from a ready-made Template, or build a page from scratch in the Builder — then export clean, production-ready code."
    />
  )
}

export default HomePage
